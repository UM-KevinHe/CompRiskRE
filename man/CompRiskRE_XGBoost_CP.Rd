% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CompRiskRE_XGBoost_CP.R
\name{CompRiskRE_XGBoost_CP}
\alias{CompRiskRE_XGBoost_CP}
\title{Competing Risks with Relative Entropy Integration (Composite Prior, XGBoost)}
\usage{
CompRiskRE_XGBoost_CP(
  prior_model,
  train_data,
  test_data,
  eta,
  xgb_params = list(objective = "multi:softprob", eval_metric = "mlogloss", num_class =
    3, max.depth = 4),
  nrounds = 100
)
}
\arguments{
\item{prior_model}{A fitted prior XGBoost model.}

\item{train_data}{A \code{data.frame} containing training data with columns:
covariates, discrete follow-up time (\code{time_y}), and event indicator (\code{event}).}

\item{test_data}{A \code{data.frame} containing test data with the same structure as \code{train_data}.}

\item{eta}{Numeric vector of RE regularization parameters.}

\item{xgb_params}{A list of XGBoost parameters. Defaults to
\code{list(objective = "multi:softprob", eval_metric = "mlogloss", num_class = 3, max.depth = 4)}.}

\item{nrounds}{Integer, number of boosting rounds for XGBoost (default: 100).}
}
\value{
A list with components:
\describe{
  \item{models}{A named list of fitted XGBoost models for \eqn{\eta = 0} and each supplied \code{eta}.}
  \item{PD}{A numeric vector of predictive deviance values on the test data.}
  \item{prior_PD}{Predictive deviance of the prior model on the test data.}
  \item{eta}{The vector of \code{eta} values, including \eqn{0} for the baseline model.}
}
}
\description{
This function implements the Relative Entropy (RE) framework for discrete-time competing risks models
where the prior model is specified in a composite failure time formulation and both prior/local models
are fit using XGBoost.
}
