% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CompRiskRE_CP.R
\name{CompRiskRE_CP}
\alias{CompRiskRE_CP}
\title{Competing Risks with Relative Entropy Integration with a Competing Risk Prior Model}
\usage{
CompRiskRE_CP(
  beta_cor = 0.9,
  eta,
  N_ext = 5000,
  N_loc = 2000,
  N_val = 200,
  N_test = 5000,
  nCause = 2,
  mu = 1,
  sigma = 0.05,
  seed = 123
)
}
\arguments{
\item{beta_cor}{Numeric correlation level between external and local models.
Must be one of \code{1.0, 0.9, 0.7, 0.5, 0.4, 0.3, 0.1, 0}.}

\item{eta}{Numeric vector of Relative Entropy regularization parameters.}

\item{N_ext}{Sample size for external data (default: 5000).}

\item{N_loc}{Sample size for local data (default: 2000).}

\item{N_val}{Sample size for validation data (default: 200).}

\item{N_test}{Sample size for test data (default: 5000).}

\item{nCause}{Number of competing causes (default: 2).}

\item{mu}{Mean of covariates (default: 1).}

\item{sigma}{Standard deviation of covariates (default: 0.05).}

\item{seed}{Random seed for reproducibility.}
}
\value{
A list with two components:
\describe{
  \item{models}{Fitted models: \code{prior}, \code{local}, \code{joint}, and
  \code{KL} (per eta value).}
  \item{metrics}{Performance results including validation/test deviance,
  C-indices, and AIC across eta values.}
}
}
\description{
This function implements the Relative Entropy (RE) framework for discrete-time competing risk models 
with the prior model is a competing risk prior model.
\itemize{
  \item Simulate external, local, validation, and test data.
  \item Fit a prior competing risks model, a local model, and a joint model.
  \item Apply Relative Entropy–based integration algorithm with direct multinomial updates.
  \item Evaluate predictive performance on validation and test datasets.
}
}
\examples{
eta <- generate_eta(method = "exponential", n = 30, max_eta = 30)
res <- CompRiskRE_CP(beta_cor = 0.9, eta = eta, seed = 2024)
names(res$metrics)

}
